// фото блюда

import { Group } from '@vkontakte/vkui';

<Group
    mode="plain"
    className={styles.imgContainer}
    separator="hide"
>
    <img scr={url} className={styles.img} />
</Group>